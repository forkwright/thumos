name: Gate verify

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      # NOTE: Placeholder until kanon gate ships trailer verification.
      # Currently passes unconditionally. Will be updated to check
      # Gate-Passed trailer once kanon gate is deployed.
      - name: Gate check (placeholder)
        run: echo "Gate verification will be enforced once kanon gate is deployed"
