name: Gate verify

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      # NOTE: Placeholder until kanon gate trailer flow is validated.
      - name: Gate check (placeholder)
        run: echo "Gate verification will be enforced once kanon gate trailer flow is validated"
