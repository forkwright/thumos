[package]
name = "klesis-core"
description = "no_std + alloc core for GSM-7 and SMS-PDU semantics (the canonical codec, address/hex primitives, and silent-SMS/WAP-Push classification, shared by klesis and the kernel)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true

[lints]
workspace = true

[package.metadata.kanon]
maturity = "alpha"
since = "2026-08-08"
exit-criteria = "operator-reviewed hardware validation and release-readiness gate"
