[package]
name = "asphaleia-core"
description = "no_std + alloc core types for packet-filter/DNS-blocklist protocol semantics (the canonical parse + surveillance-domain policy, shared by asphaleia and the kernel)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true

[lints]
workspace = true

[package.metadata.kanon]
maturity = "alpha"
since = "2026-08-08"
exit-criteria = "operator-reviewed hardware validation and release-readiness gate"
