# Target-executed test ledger (issue #551, restoring the #124 obligation)
# Per test-bearing (or target-sensitive) module: the declared execution
# mechanism (host / target / both), the witness script(s) covering the
# target side, and the fidelity limits of any host fixture. Enforced by
# scripts/check-target-test-ledger.sh: every such module has a row, 'both'
# rows name real witness scripts, sensitive host-only rows carry a fidelity
# note, and the tests count matches the RUNNABLE set `cargo nextest list`
# reports for the i686 host test binary (#645) -- not a count of `#[test]`
# occurrences in source text, which cannot tell a module a #[cfg] excluded
# from every test build from one that genuinely compiled and ran.
#
# #528/#533 are the proof-by-incident: host fixtures green while real-table
# behavior was broken on every boot — host tests are structurally blind to
# the section-overlay class. The 'both' rows are where that class lives.
# #619/#631/#645 are the proof-by-incident for the tests-count rule itself:
# emmc (31 tests) and the then-three watchdog tests were both cfg'd out of
# every test build. Their tests existed as text, were counted as coverage, and
# had never once been compiled -- exactly the failure mode a text-based count
# cannot see.

[[module]]
name = "audio"
tests = 29
mechanism = "both"
witness = "boot.sh"
fidelity = "QEMU exercises AudioManager with NullCodec; #876 owns the source-grounded modem/call-audio path and calibrated echo criterion"

[[module]]
name = "audio_codec"
tests = 18
mechanism = "both"
witness = "boot.sh"
fidelity = "QEMU uses NullCodec; the non-QEMU Mt6357Codec now returns PmicUnavailable without PMIC/AP MMIO, while #862 owns the source-grounded PWRAP transaction seam"

[[module]]
name = "audio_route"
tests = 22
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "audit"
tests = 27
mechanism = "host"
fidelity = "Live-loop HMAC chaining uses a volatile per-boot key; #863 owns persistent key/head/epoch and #878 owns storage rollback resistance"

[[module]]
name = "avdtp"
tests = 2
mechanism = "host"

[[module]]
name = "battery"
tests = 11
mechanism = "host"
fidelity = "Generic lookup/poll logic only; #877 owns a source-grounded PMIC interface and calibrated state-of-charge model before production wiring"

[[module]]
name = "bfu_timer"
tests = 17
mechanism = "host"

[[module]]
name = "block"
tests = 23
mechanism = "host"

[[module]]
name = "gpt"
tests = 8
mechanism = "host"
fidelity = "Parse logic is proven on synthetic GPT images host-side; #870 must source-ground FIFOCS.RXCNT before the live eMMC GPT read may execute, after which an operator-owned M7 receipt can qualify it"

[[module]]
name = "bluetooth"
tests = 25
mechanism = "host"

[[module]]
name = "briar"
tests = 30
mechanism = "host"

[[module]]
name = "bt_audio"
tests = 22
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "cache"
tests = 11
mechanism = "host"

[[module]]
name = "capability"
tests = 8
mechanism = "host"

[[module]]
name = "ccci"
tests = 77
mechanism = "host"
fidelity = "Host tests cover the frame codec; #398 must implement the current hard-stubbed CCCI-to-AT transport before an operator-owned modem/M7 receipt"

[[module]]
name = "ccci_logger"
tests = 42
mechanism = "host"

[[module]]
name = "clock"
tests = 27
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "console"
tests = 4
mechanism = "host"
fidelity = "Host-testability and the historical #459 presence-sequence repair are covered; #865 owns the remaining MT6739 RX-ready/FIFO and presence-timing source acceptance"

[[module]]
name = "contacts"
tests = 25
mechanism = "host"

[[module]]
name = "csprng"
tests = 19
mechanism = "both"
witness = "boot.sh"
fidelity = "Host tests drive the estimator with synthetic sample sequences and the deterministic seed path, so they pin what it refuses and what it admits but never observe real ISR-latency jitter; #873 owns non-repeating reseed state before any device-bound entropy health receipt"

[[module]]
name = "devfs"
tests = 27
mechanism = "host"

[[module]]
name = "board"
tests = 1
mechanism = "host"
fidelity = "shared memory-map tables pinned host-side; MMIO interaction is the m7 boot path (hardware-gated)"

[[module]]
name = "board/m7"
tests = 3
mechanism = "host"
fidelity = "address tables + registry population pinned host-side; MMIO interaction is the m7 boot path (hardware-gated)"

[[module]]
name = "board/virt"
tests = 0
mechanism = "host"
fidelity = "virt consts compile only under --features qemu (the armv7a witness builds); the boot witness exercises the virt registry"

[[module]]
name = "device"
tests = 6
mechanism = "host"

[[module]]
name = "dhcp"
tests = 4
mechanism = "host"

[[module]]
name = "display"
tests = 32
mechanism = "host"
fidelity = "Synthetic framebuffer covers rendering; #854 must resolve/source-ground the DBI/DSI GC9306 transport before any DDP/panel hardware witness"

[[module]]
name = "dns"
tests = 29
mechanism = "host"

[[module]]
name = "dns_tls"
tests = 32
mechanism = "host"

[[module]]
name = "ekphrasis"
tests = 64
mechanism = "host"

[[module]]
name = "elf"
tests = 18
mechanism = "both"
witness = "boot.sh+exec.sh+forkexec.sh"
fidelity = "loader correctness against real page tables is witness-only (exec/forkexec); host tests cover header/segment parsing"

[[module]]
name = "emmc"
tests = 42
mechanism = "both"
witness = "boot.sh"
fidelity = "Host tests cover protocol logic; #870 must source-ground FIFOCS.RXCNT before PIO reads, then an operator-owned eMMC receipt may qualify device behavior"

[[module]]
name = "encryption"
tests = 15
mechanism = "host"
fidelity = "XTS confidentiality mechanics are host+fuzz covered; #866 owns the plaintext compatibility mount and #878 owns persistent integrity/freshness/rollback resistance before device qualification"

[[module]]
name = "exceptions"
tests = 0
mechanism = "target"
witness = "boot.sh+kfault.sh+uaccess.sh"

[[module]]
name = "exceptions_stub"
tests = 4
mechanism = "host"
fidelity = "the stub IS the host fixture for the exceptions module — see exceptions for the witness side"

[[module]]
name = "fd"
tests = 73
mechanism = "host"
fidelity = "copyout-fault tests preserve file data and offsets; /dev/urandom may advance volatile PRNG state if a transfer faults after prevalidation"

[[module]]
name = "firewall"
tests = 30
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "fm_radio"
tests = 25
mechanism = "host"

[[module]]
name = "futex"
tests = 8
mechanism = "both"
witness = "sleep.sh"
fidelity = "WAIT/WAKE logic host-covered; the IRQ-masked sleep/wake path is sleep-witnessed"

[[module]]
name = "gic"
tests = 0
mechanism = "host"
fidelity = "register interaction is not exercised host-side; the PL0 probes cover the live GIC path"

[[module]]
name = "gps"
tests = 19
mechanism = "host"

[[module]]
name = "harmostes"
tests = 57
mechanism = "host"

[[module]]
name = "heap"
tests = 0
mechanism = "target"
witness = "brk.sh"

[[module]]
name = "heorte"
tests = 17
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "heorte_alarm"
tests = 7
mechanism = "host"

[[module]]
name = "heorte_timer"
tests = 14
mechanism = "host"

[[module]]
name = "http_client"
tests = 47
mechanism = "host"

[[module]]
name = "ipc"
tests = 11
mechanism = "host"

[[module]]
name = "irq"
tests = 4
mechanism = "both"
witness = "boot.sh"
fidelity = "GIC register interaction is not exercised host-side; the PL0 probes cover the live IRQ path"

[[module]]
name = "json_mini"
tests = 66
mechanism = "host"

[[module]]
name = "kardia"
tests = 0
mechanism = "target"
witness = "boot.sh+crashloop.sh"

[[module]]
name = "kconfig"
tests = 3
mechanism = "host"
fidelity = "constant table only — nothing target-executed to diverge"

[[module]]
name = "key_manager"
tests = 16
mechanism = "host"
fidelity = "Deterministic KDF mechanics are host-covered; #872 owns boot-secret/KDF acceptance and #873 owns non-repeating CSPRNG reseed state before device qualification"

[[module]]
name = "keypad"
tests = 6
mechanism = "host"
fidelity = "Scan/debounce logic is proven on injected matrices host-side; #880 owns source-grounded AGM M7 pins, GPIO transactions, pin-conflict/ghosting cases, and fail-closed disablement of the current placeholder adapter before an operator-owned keypad receipt"

[[module]]
name = "kinit"
tests = 0
mechanism = "target"
witness = "boot.sh"

[[module]]
name = "kinit_plan"
tests = 30
mechanism = "host"
fidelity = "The boot decisions here are pure functions over observed state, so host tests prove them exactly — including #360's encrypted-format gate, whose load-bearing case is a device provisioned in an EARLIER boot being refused. What host tests cannot reach is whether kinit.rs feeds these functions the right state on real hardware: `provisioned_this_boot` is set inside the non-QEMU first-boot-setup arm, which no host or QEMU profile executes. boot.sh witnesses kinit, not that arm."

[[module]]
name = "liveness"
tests = 12
mechanism = "host"
notes = "The gate is pure and fully host-tested; what the host cannot reach is the timer IRQ and service loop that feed it, both ARM-only. So these tests prove the RULES -- deadline, per-owner stall, recovery, wrap, shutdown bound -- and the QEMU boot witness proves the wiring by booting at all, since a gate that never pets resets the board."

[[module]]
name = "lfs"
tests = 49
mechanism = "host"
fidelity = "Host LFS behavior is covered, but boot currently auto-formats on ambiguous InvalidSuperblock; #360 must distinguish authenticated first provisioning from corrupt/version-incompatible existing storage before any device persistence receipt"

[[module]]
name = "lfs_checkpoint"
tests = 9
mechanism = "host"

[[module]]
name = "lfs_compact"
tests = 11
mechanism = "host"

[[module]]
name = "lfs_imap"
tests = 14
mechanism = "host"

[[module]]
name = "lfs_segment"
tests = 14
mechanism = "host"

[[module]]
name = "lfs_writer"
tests = 7
mechanism = "host"

[[module]]
name = "lock_screen"
tests = 24
mechanism = "host"
fidelity = "Policy logic is host-covered; #841 owns the weak latent lock-screen verifier and #872 owns accepted boot/long-sleep secret and KDF policy"

[[module]]
name = "main"
tests = 0
mechanism = "target"
witness = "boot.sh"
fidelity = "module wiring only — the boot witness is its proof"

[[module]]
name = "matrix_crypto"
tests = 54
mechanism = "host"
fidelity = "the target-sensitive marker is write_volatile used ONLY to scrub superseded ratchet and skipped-key material (#830) -- not MMIO, page tables, or any address layout -- so the host run executes the same code with the same semantics and there is no target-only behaviour a boot witness would expose. What a host test genuinely cannot prove is that no copy survives elsewhere (a spilled register, a moved-from temporary): write_volatile bounds the store it is given, not the liveness of the value. That limit is identical on host and target, which is why this stays host-only."

[[module]]
name = "matrix_ids"
tests = 11
mechanism = "host"

[[module]]
name = "memguard"
tests = 31
mechanism = "both"
witness = "uaccess.sh"
fidelity = "Host tests pin descriptor permission decoding (including execute-only data denial), holes/boundaries, the live-VAS versus bootstrap identity-map policy split, and post-validation copyout rollback through fault injection. QEMU drives real low anonymous mappings through UART write and clock_gettime syscalls (RW positive controls, PROT_NONE, read-only direction split, unmapped and cross-page tails), proves the inexpressible ARMv7 execute-only mapping is refused, then separately bypasses prevalidation to fault and recover the actual ARM LDRBT/STRBT transfer instructions. kfault.sh remains the unrelated-PL1-fault negative control."

[[module]]
name = "meshtastic"
tests = 36
mechanism = "host"

[[module]]
name = "metaxu_bridge"
tests = 4
mechanism = "host"

[[module]]
name = "mic_audit"
tests = 18
mechanism = "host"

[[module]]
name = "mmio"
tests = 4
mechanism = "host"
fidelity = "register access helpers — production paths are board/hardware-gated"

[[module]]
name = "mmu"
tests = 36
mechanism = "both"
witness = "fork.sh+exec.sh+guard.sh+brk.sh"

[[module]]
name = "net"
tests = 18
mechanism = "host"

[[module]]
name = "nous"
tests = 45
mechanism = "host"

[[module]]
name = "page"
tests = 9
mechanism = "both"
witness = "fork.sh+guard.sh"
fidelity = "real-table behavior is fork/guard-witnessed; host fixtures use absent-entry tables (#533 class)"

[[module]]
name = "panic_wipe"
tests = 18
mechanism = "host"

[[module]]
name = "passphrase_policy"
tests = 11
mechanism = "host"
fidelity = "The policy is pure arithmetic over typed digits, so host tests prove acceptance and refusal exactly. What they cannot reach is the boot-setup arm that feeds it: kinit.rs's first-boot loop runs only on non-QEMU hardware under secure_boot_ok, which no host or QEMU profile executes -- the same limit kinit_plan's row records. Generation is exercised against the seeded test CSPRNG, not device entropy."

[[module]]
name = "pipe"
tests = 16
mechanism = "host"
fidelity = "Host tests prove prevalidation leaves the pool untouched, an injected post-validation fd-pair copyout fault rolls back pipe/fd/OFD publication, and an injected read copyout fault preserves queued bytes for the next read. Caller-VAS permission and real ARM abort recovery are exercised through memguard's uaccess.sh target witness; no QEMU pipe transport scenario is claimed by this row."

[[module]]
name = "power"
tests = 30
mechanism = "host"
fidelity = "Radio request policy and a test-only logical CPU policy candidate are host-covered. Runtime CPU requests/actuation are absent; #879 owns source-grounded policy, transactions, readback, and qualification. #874 owns radio requested/applied/observed semantics and automatic-request removal; #862 owns valid PWRAP actuation."

[[module]]
name = "process"
tests = 53
mechanism = "both"
witness = "boot.sh+fork.sh+exec.sh+forkexec.sh+guard.sh+brk.sh+crashloop.sh"

[[module]]
name = "provision"
tests = 33
mechanism = "host"

[[module]]
name = "qemu"
tests = 0
mechanism = "target"
witness = "boot.sh"
fidelity = "emulation shim itself — exercised by every witness run"

[[module]]
name = "ramfs"
tests = 35
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "reflex"
tests = 3
mechanism = "host"

[[module]]
name = "sbc"
tests = 9
mechanism = "host"

[[module]]
name = "screen_alarm"
tests = 16
mechanism = "host"

[[module]]
name = "screen_calendar"
tests = 8
mechanism = "host"

[[module]]
name = "screen_call"
tests = 16
mechanism = "host"

[[module]]
name = "screen_contacts"
tests = 16
mechanism = "host"

[[module]]
name = "screen_dialer"
tests = 14
mechanism = "host"

[[module]]
name = "screen_fm"
tests = 15
mechanism = "host"

[[module]]
name = "screen_home"
tests = 19
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "screen_messages"
tests = 42
mechanism = "host"

[[module]]
name = "screen_nous"
tests = 42
mechanism = "host"

[[module]]
name = "screen_privacy"
tests = 33
mechanism = "host"

[[module]]
name = "screen_radio"
tests = 13
mechanism = "host"

[[module]]
name = "screen_search"
tests = 14
mechanism = "host"

[[module]]
name = "screen_settings"
tests = 14
mechanism = "host"

[[module]]
name = "screen_threat"
tests = 24
mechanism = "host"

[[module]]
name = "screen_unimplemented"
tests = 4
mechanism = "host"

[[module]]
name = "secure_boot"
tests = 28
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "secrets"
tests = 14
mechanism = "host"
fidelity = "Provision/read is proven on the RAM double; #866 blocks plaintext userdata setup, #870 blocks the ungrounded eMMC read field, #878 owns authenticated/versioned rollback-resistant persistence, and #872 owns boot-secret acceptance before an operator-owned M7 receipt"

[[module]]
name = "security"
tests = 35
mechanism = "host"
notes = "The Argon2id block matrix comes from a host allocation under cfg(test): the page allocator's addresses are not mapped on this target, so the page-backed production path is exercised only on device. What the host does cover is the body around it -- page-count arithmetic, the scrub before release, and the refusal when the matrix is unavailable (#272)."

[[module]]
name = "security_mode"
tests = 39
mechanism = "both"
witness = "boot.sh"
fidelity = "Policy transitions are host/QEMU-covered, but #874 blocks automatic modem-cut semantics and requested/applied/observed conflation; #881 owns the absent physical side-button/PTT producer and safe gesture policy"

[[module]]
name = "signal"
tests = 15
mechanism = "both"
witness = "signal.sh"

[[module]]
name = "sim"
tests = 20
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "slab"
tests = 15
mechanism = "host"
fidelity = "allocator-on-real-tables is brk-witnessed; host tests cover size-class logic"

[[module]]
name = "sms"
tests = 32
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "socket"
tests = 25
mechanism = "host"
fidelity = "copyout-fault tests retain TCP/UDP queues; disjoint recvfrom outputs are prevalidated but cannot be byte-atomic across a late transfer fault"

[[module]]
name = "status_bar"
tests = 13
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "supervisor"
tests = 12
mechanism = "both"
witness = "crashloop.sh"

[[module]]
name = "syscall"
tests = 72
mechanism = "both"
witness = "boot.sh+fork.sh+exec.sh+sleep.sh+uaccess.sh"
fidelity = "trap entry / exception return is witness-only (PL0 probes); host tests cover dispatch logic via the uart stub"

[[module]]
name = "t9"
tests = 14
mechanism = "host"

[[module]]
name = "telephony"
tests = 40
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "telephony_mock"
tests = 0
mechanism = "target"
witness = "boot.sh"

[[module]]
name = "telephony_parser"
tests = 19
mechanism = "host"

[[module]]
name = "time"
tests = 13
mechanism = "both"
witness = "uaccess.sh"
fidelity = "Host tests cover ClockManager wiring and offset math; uaccess.sh drives a real clock_gettime copyout into RW and read-only caller mappings to prove direction enforcement. #861 owns source/authentication/freshness policy and explicit unavailable civil time"

[[module]]
name = "timer"
tests = 0
mechanism = "target"
witness = "boot.sh"
fidelity = "Host tests exercise timer_stub; the tick-driven QEMU loop is boot-witnessed, while physical CNTPCT/timer behavior remains a later operator receipt"

[[module]]
name = "timer_stub"
tests = 5
mechanism = "host"
fidelity = "the stub is the host fixture — see timer"

[[module]]
name = "uart"
tests = 0
mechanism = "target"
witness = "boot.sh"
fidelity = "The stub covers host tests; #865 must source-ground RX-ready/FIFO semantics and presence timing before physical UART qualification"

[[module]]
name = "uart_pl011"
tests = 0
mechanism = "host"
fidelity = "PL011 is the QEMU-only board path exercised by the boot witness; non-QEMU M7 builds select the MediaTek UART path tracked by #865"

[[module]]
name = "uart_stub"
tests = 0
mechanism = "host"

[[module]]
name = "ui"
tests = 30
mechanism = "both"
witness = "boot.sh"

[[module]]
name = "usb"
tests = 34
mechanism = "host"
fidelity = "MMIO/GIC dispatch is not exercised host-side: board::m7::MUSB_IRQ remains None until #676 accepts source provenance for candidate INTID 105 or records a physical witness. SERIAL_RX_RING_LOCK contention is exercised host-side under real OS-thread concurrency, not single-core simulation."

[[module]]
name = "vfs"
tests = 19
mechanism = "host"

[[module]]
name = "watchdog"
tests = 4
mechanism = "host"
fidelity = "Host tests cover register/arming logic; #875 must couple petting to scheduler/service-loop progress before a physical reset/deadline receipt"

[[module]]
name = "watchdog_qemu"
tests = 0
mechanism = "host"

[[module]]
name = "wifi"
tests = 15
mechanism = "host"
fidelity = "WPA/PTK logic is host+fuzz covered; #129 production WMT/STP radio I/O is missing before hardware qualification"
