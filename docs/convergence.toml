# Convergence ledger (issue #545) — the machine-checked record of the
# kernel↔workspace duplication and its dispositions. scripts/check-convergence.sh
# enforces: every pair here has a disposition + owner, every "ported from"
# comment names a live pair below, and the #126 dead-code expectations only
# ever burn DOWN (the ratchet — the counts below may not increase).
#
# Dispositions:
#   converged        — one canonical implementation owns the invariant (name it)
#   extract-core     — pull the shared protocol/state machine into a
#                      no_std+alloc core crate both sides link (topology 1)
#   kernel-owned     — the kernel copy is canonical; the workspace duplicate
#                      deletes when its consumers are gone (topology 2)
#   workspace-owned  — the workspace crate is canonical; the kernel copy is a
#                      transport/board adapter only
#
# The ratchet: [ratchet].ported_from_comments below is the count, and
# scripts/check-convergence.sh derives the live figure from the tree and fails
# if it ever rises. WHY this header names no number: a count restated here is a
# second copy that drifts the moment a pair converges — this one said 6 while
# the ratchet said 3 and one of the files it named had already been deleted.
# A doc-header inventory of a thing a script already measures is drift waiting
# to happen; read the ratchet and the per-pair notes instead.

[[pair]]
name = "threat-semantics"
kernel = "screen_threat.rs (was: divergent bands 25/50/75)"
workspace = "sema (cell.rs ThreatLevel, canonical bands 30/60/80)"
disposition = "converged"
canonical = "sema-core (no_std+alloc; kernel links via path dep, sema re-exports)"
owner = "#545 (this PR)"
notes = "The live divergence that proved the class: screen bands and the detector's protocol invariants disagreed. Now one type, one boundary table, both consumers."

[[pair]]
name = "keypad-matrix-scan"
kernel = "keypad.rs (mirrors haphe's scan/debounce algorithm, #446)"
workspace = "haphe (gpio.rs)"
disposition = "kernel-owned"
canonical = "keypad.rs's BootKeypad is the only consumed matrix-scan/debounce implementation; haphe::gpio's GpioKeypad/Debounce/KeyMatrix are pub(crate) and reached only by their own test module, so the duplicate has no consumer to converge with"
owner = "#545 (this PR)"
notes = "The event models genuinely differ: BootKeypad::poll_with_matrix (keypad.rs) returns Option<Key>, the first confirmed press only, no queue, no release/held -- it runs before interrupts, the heap, or the real KPD hardware block exist. GpioKeypad::process_matrix (haphe/gpio.rs) pushes InputEvent::Key { key, state: Pressed | Released } into a 64-slot InputQueue for a running input loop. But no second consumer exists to justify lifting a shared core: GpioKeypad/Debounce/KeyMatrix/gpio_reg/read_matrix are pub(crate), unreachable outside haphe, and called only from gpio.rs's own tests -- eidolon, haphe's one real external consumer, imports haphe::input::{Key, TouchPoint} and never touches haphe::gpio. Pin assignments are shared placeholders pending AGM M7 schematic verification either way. haphe has been #![no_std] with zero dependencies since its first commit and crates/thumos/Cargo.toml already path-links it as a dev-dependency for host tests (#615) -- \"cannot link a std crate\" is not the real reason keypad.rs stays independent; visibility and the differing event shape are. The workspace matrix-scan code is a deletion candidate under this disposition's own rule (the duplicate deletes when its consumers are gone) whenever a real second consumer is decided against; it is kept here only because deleting it is outside this ledger decision's scope."

[[pair]]
name = "gps"
kernel = "gps.rs (checksum framing, coordinate/fix-quality, GGA/RMC parsing converged)"
workspace = "topos"
disposition = "converged"
canonical = "topos-core (no_std+alloc; kernel links via path dep, topos re-exports FixQuality and converts Position/Fix)"
owner = "#545 (this PR)"
notes = "Converged: $...*XX checksum framing, ddmm.mmmm coordinate conversion, FixQuality, and full GGA/RMC sentence parsing. Five divergences found and resolved on the way -- (1) topos's checksum parser accepted a checksum field with only one hex digit (a width-flexible hex reader validated a truncated sentence missing its leading zero); the kernel required exactly two and topos now does too. (2) the kernel widened a parsed GGA quality byte via a truncating `as u8` cast, so a quality field of 264 wrapped to 8 (a DEFINED code) instead of being rejected; converged onto a saturate-to-default parse. (3) the kernel's fixed-point coordinate parser rejected minutes >= 60 and guarded an unbounded fractional-digit exponent; topos's f64 parser did neither -- both now share the guarded parser. (4) neither side bounded latitude to +/-90 or longitude to +/-180; both now do. (5) the kernel's altitude parser had no sign handling and silently swallowed a legitimate below-sea-level reading; topos's f64 parser handled it correctly and the kernel now does too. Also found: topos's RMC parser never extracted time/date at all (no prior implementation to compare against; both sides now share it, with range validation on hour/minute/second/month/day that neither side had, since clock.rs treats GPS as its highest-trust automatic time source); and topos's own `parse_gga_returns_error_when_no_fix` test carried a hardcoded checksum that did not match its body text, so it passed for the wrong reason (exercising the checksum-mismatch path, not the no-fix path it claimed to cover) -- fixed to compute the checksum. NOT converged: the WMT/CCCI hardware transport (SentenceBuffer, GpsHwOps, GpsHw) and the SentenceBuffer/GpsReceiver lifecycle state machine, which are genuinely kernel-device-specific; topos's own HDOP/speed/course extraction, which is plain `str::parse` with no NMEA-specific invariant worth sharing and which the kernel never reads."

[[pair]]
name = "wifi"
kernel = "wifi.rs (ported from aither)"
workspace = "aither"
disposition = "extract-core"
canonical = "pending: aither-core (scan/association state machine)"
owner = "unscheduled"
notes = "wifi.rs:3 doc header records the port."

[[pair]]
name = "bluetooth"
kernel = "bluetooth.rs (ported from pteron)"
workspace = "pteron"
disposition = "workspace-owned"
canonical = "pteron (RPA/privacy already canonical there, #455); kernel keeps the HCI board transport"
owner = "unscheduled"
notes = "bluetooth.rs:3 doc header records the port. After #455 the workspace side is the richer, spec-verified side."

[[pair]]
name = "telephony-sms"
kernel = "telephony.rs, telephony_parser.rs, sms.rs, sim.rs (CCCI/CLDMA transport, +CREG's <lac>/<ci> vs <AcT> composite shapes, and +CMTI/+COPS/+CPINR stay kernel-local; +CPIN? response classification converged, #696; SMS BCD address digit packing converged, #545)"
workspace = "klesis"
disposition = "converged"
canonical = "klesis-core (GSM-7 codec, PDU byte primitives, surveillance classification; RegStatus; SimPinState +CPIN? classifier; AT final-result/RING token exactness, +CSQ extraction, dial-string charset, and dBm/bars signal conversion)"
owner = "#545 (this PR)"
notes = "Converged (#662): the GSM-7 tables and codec, hex/BCD/cursor primitives, and the silent-SMS + WAP-Push classification. The kernel's duplicate gsm7.rs is deleted. Two live divergences were found and resolved on the way -- the kernel discarded the PID entirely, so silent SMS was detected only in the layer that never runs; and the kernel accepted GSM-7 data ending on a dangling ESC that klesis rejected. Converged (#545, this PR): RegStatus and its From<u8> table; the OK/ERROR/RING exact-token classifier and +CME ERROR:/+CMS ERROR: numeric-code extraction (#685); +CSQ rssi/ber extraction; the dial-string charset (is_valid_dial_byte, shared by klesis's outbound validate_phone_number and the kernel's inbound +CLIP validation); and the RSSI-to-dBm/dBm-to-bars signal conversion, previously kept in sync only by a comment (sim.rs carried a THIRD independent copy of the RSSI-to-dBm formula, outside this pair's named files but the same primitive -- also converged onto klesis_core rather than left as a newly-more-visible stale duplicate). One divergence found and resolved on the way: klesis's parser rejected a CME/CMS error body that was not cleanly numeric (`+CME ERROR: 10x`, or verbose AT+CMEE=2 text) outright, where the kernel already classified it as a generic error (finding 13) -- rejecting left klesis's own response loop treating the line as informational and looping for a final result that would never arrive, the same silent-timeout hazard finding 13 fixed on the kernel side. klesis now inherits the kernel's (already-tested) behavior. Converged (#696): the `+CPIN?` SIM lock-state classifier (SimPinState, parse_cpin) -- telephony_parser.rs's classifier and sim.rs's independent second classifier (parse_cpin_ready/parse_cpin_sim_pin) both matched SIM PIN/SIM PUK by prefix, so `+CPIN: SIM PUK2` (a PIN2/FDN lock, a distinct 3GPP TS 27.007 §8.3 value) took the SIM PUK arm and reported the primary-SIM PUK state; a PUK has a small lifetime attempt limit with no carrier reset, so an unlock flow built on that misclassification burns attempts against the wrong credential. Both kernel classifiers are deleted; both now delegate to klesis_core::parse_cpin, which matches by exact equality (no prefix, no shadowing order to get wrong) and adds SimPinState::Pin2Required/Puk2Required as states distinct from the primary PIN/PUK states. klesis has no `+CPIN?` response consumer of its own (CPIN_QUERY sends the command but no caller classifies the reply), so this is a one-sided convergence in the sense the `keypad-matrix-scan` pair describes -- but unlike that pair, the shared classifier is not orphaned: it has two real consumers within the kernel crate (telephony_parser.rs and sim.rs), which is what made the duplication a live defect rather than dead code. Converged (#545, this PR): sms.rs's BCD-address port marker -- klesis/src/pdu.rs carried an independent, fully re-implemented decode_bcd_address/encode_bcd_address that duplicated the digit-pair packing/unpacking klesis_core already exposed (and the kernel already consumed). klesis's decode now calls klesis_core::decode_bcd_address directly (identical behavior: '+' is prepended iff the type-of-address octet is 0x91, exactly klesis's prior rule). klesis's encode now calls a new klesis_core::pack_bcd_digits -- factored out of the already-shared klesis_core::encode_bcd_address so both the type-derived (klesis) and `+`-derived (kernel, klesis_core::encode_bcd_address) callers pack through the one implementation. Two tightenings found on the way, both fail-closed: klesis's own encode previously capped only at u8::MAX (255 digits) where klesis_core enforces the 20-digit 3GPP TS 23.040 ceiling the kernel already observed, and klesis's own decode had no digit-count cap at all; both now inherit the shared 20-digit bound (unreachable in practice -- SMS addresses never carry more digits -- but no longer a latent gap). NOT converged, by design: +CREG's composite URC shape (klesis needs `<lac>`/`<ci>`; the kernel needs `<AcT>`; the shared code table is RegStatus, already converged); +CMTI, +COPS, +CPINR, which exist on only one side; the CCCI/CLDMA hardware transport; and klesis's `Address`/`AddressType` type-of-address classification (International/National/Unknown) and its round-trip through the type-of-address octet on encode -- klesis preserves a caller-supplied type across decode/re-encode (e.g. a non-standard 0x80 'unknown' TOA it received), which the kernel has no equivalent for and which a bare `+`-prefix-driven encode (klesis_core::encode_bcd_address's own rule, used directly by the kernel) cannot reconstruct."

[[pair]]
name = "firewall"
kernel = "firewall.rs (packet parse still kernel-local; DNS + blocklist converged)"
workspace = "asphaleia"
disposition = "extract-core"
canonical = "asphaleia-core (DNS QNAME extraction, blocklist policy + suffix rule); packet-parse core still pending"
owner = "unscheduled"
notes = "Converged (#661 workspace side, #662 kernel side): SURVEILLANCE_DOMAINS, domain_matches_suffix, normalize_suffix, and extract_query_domain. WHY the two-step: #661 created the core, wired crates/asphaleia, and added the dependency to the kernel WITHOUT importing it -- so the blocklist divergence stayed live on the side that actually filters packets, and the `*.` fail-open fix never reached it. The checker now fails on a declared *-core path dependency that no source file names, which is the tell that catches this class. NOT yet converged: parse_packet, because the kernel's PacketInfo is a deliberately narrower shape than the core's Ipv4Fields/TcpFields/UdpFields. Live port marker: firewall.rs:602."

[[pair]]
name = "aletheia-bridge"
kernel = "metaxu_bridge.rs (new, #544 on-device leg)"
workspace = "metaxu"
disposition = "converged"
canonical = "metaxu-core (envelope framing, signed-grant verification, typed task/response payloads, authenticated-session logic; no_std+alloc, kernel links via path dep, metaxu re-exports)"
owner = "#544"
notes = "Born shared, not reconciled from a divergence: the kernel had zero metaxu-related code before #544, so this is the #545 topology applied at the crate's creation rather than an after-the-fact extraction. metaxu's own envelope.rs/grants.rs/protocol.rs/session.rs moved into metaxu-core verbatim (session.rs stays a thin std-error-mapping wrapper in metaxu); metaxu_bridge.rs (kernel, metaxu-probe feature only) is the second consumer."

[[pair]]
name = "ui"
kernel = "ui.rs, screen_dialer.rs (font behavior, dialer rendering ported from eidolon/haphe)"
workspace = "eidolon, haphe"
disposition = "extract-core"
canonical = "eidolon-core for display layout geometry (#740) and the 8x16 bitmap font table + CHAR_WIDTH/CHAR_HEIGHT/FONT_FIRST/FONT_LAST/FONT_CHAR_COUNT (#545); colour type and drawing-primitive/widget core still pending"
owner = "unscheduled"
notes = "Live 'ported from' comment: screen_dialer.rs:11. eidolon -> haphe is the one real internal edge; the kernel consumes neither today. ui::Key vs haphe::input::Key (#615): the digit/star/hash/d-pad prefix (0-15) is genuine shared vocabulary, pinned by ui::tests::shared_prefix_discriminants_match_haphe against the real haphe crate; the extended range (16-21) is two independent button sets that happen to share a numeric range and is deliberately NOT asserted equal. Converged (#545, this slice): FONT_DATA (95 glyphs, verified byte-identical between ui.rs and eidolon/src/font.rs before the move -- the only prior difference was a `pub(crate)` visibility modifier) and its five dimension constants. Both sides now alias eidolon_core's copy via the same const-derivation pattern #740 established for the layout constants, never `pub(crate) use`: the kernel's nine feature configurations turn an unused re-export's `unused_imports` into a build failure that the crate-level `dead_code` expectation in main.rs does not cover. Also corrected three doc comments that had survived #731/#732 claiming eidolon or haphe 'cannot be linked' into the `#![no_std]` kernel because of a `std` dependency -- both crates have been `#![no_std]` since #732 (eidolon) and their first commit (haphe), and the kernel already path-links eidolon-core (and haphe as a dev-dependency, #615); the real reasons the colour type and ui::Key stay duplicated are unrelated to std and are unaffected by this slice (see below). NOT converged, deliberately: the colour type (Rgb565 lives in `eidolon`, which the kernel does not link -- only `eidolon-core` -- and converging it would ripple across ~19 kernel call sites that pass a raw u16 fg/bg); set_pixel/draw_char/draw_str/draw_char_scaled and ui.rs's test module (owned by a parallel lane fixing a live defect in the drawing primitives, #760); and ui::Key (vocabulary divergence above discriminant 15, as above)."

# The #126 ratchet: the dead-code expectations pointing at closed #126
# (one per workspace crate lib.rs) now point here. They burn down as each
# pair converges — the checker fails if the count ever increases.
[ratchet]
rule = "port-markers-and-126-expectations-never-increase"
ported_from_comments = 2      # firewall (packet parse), screen_dialer
stale_126_expectations = 0    # all workspace lib.rs pointers now name this ledger, not closed #126
