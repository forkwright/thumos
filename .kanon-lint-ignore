# release-please auto-generates CHANGELOG.md from conventional commits.
# REPO-SETUP.md says to omit it, but release-please creates it automatically
# and removing it would break the release workflow.
RELEASES/changelog-present:CHANGELOG.md
# DISCLAIMER.md is required by thumos's legal status (research/educational software).
RELEASES/disclaimer-present:DISCLAIMER.md

# =============================================================================
# Workspace library crates — public API for the out-of-workspace kernel binary
# =============================================================================
#
# WHY(#126): The thumos kernel binary is excluded from the workspace (bare-metal
# target, separate Cargo.toml). The 12 workspace library crates below are the
# intended consumer-facing API surface — items declared `pub` are consumed by
# the binary and by external consumers via cargo publish. Narrowing to
# `pub(crate)` would trip clippy's dead-code detection for every non-internally-
# used item, because the workspace cannot see the binary's consumption. Once
# the binary imports these crates as path deps (#126 tracks that work), each
# crate's pub surface can be audited and narrowed per-item.
RUST/pub-visibility:crates/eidolon/**
RUST/pub-visibility:crates/krypta/**

# =============================================================================
# Parser/encoder direct indexing in fixed-size buffers
# =============================================================================
#
# WHY(#130): Indexing into local stack-allocated fixed-size arrays with literal
# indices, or into slices after an explicit `ensure!` bound check. The lint
# cannot prove the check precedes the access, but each call site is demonstrably
# safe. Tracked for eventual rewrite via destructuring / to_le_bytes / get_mut.
RUST/indexing-slicing:crates/aither/src/mac.rs
RUST/indexing-slicing:crates/aither/src/eapol.rs
RUST/indexing-slicing:crates/pteron/src/transport.rs
RUST/indexing-slicing:crates/pteron/src/hci.rs
RUST/indexing-slicing:crates/kelyphos/src/stp.rs
RUST/indexing-slicing:crates/kelyphos/src/transport.rs
RUST/indexing-slicing:crates/klesis/src/pdu.rs
RUST/indexing-slicing:crates/klesis/src/ccci.rs
RUST/indexing-slicing:crates/klesis/src/gsm7.rs
RUST/indexing-slicing:crates/klesis/src/transport.rs
RUST/indexing-slicing:crates/haphe/src/gpio.rs
RUST/indexing-slicing:crates/haphe/src/input.rs
RUST/indexing-slicing:crates/haphe/src/touch.rs
RUST/indexing-slicing:crates/krypta/src/x3dh.rs
RUST/indexing-slicing:crates/leipsanon/src/memory.rs
RUST/indexing-slicing:crates/leipsanon/src/trigger.rs
RUST/indexing-slicing:crates/stegnos/src/cipher.rs
RUST/indexing-slicing:crates/stegnos/src/erase.rs

# =============================================================================
# String/byte slice indexing and enum-as-cast in parsers
# =============================================================================
#
# WHY(#130): `&buf[..N]` and `&payload[pos..pos+len]` slicing after explicit
# length checks; `enum as u8` for enum→wire discriminant conversions where the
# discriminant values are 0..N; bit-shift `as u16` conversions whose domain is
# proven by the source type's bit width. Same tracking issue as indexing-slicing.
RUST/string-slice:crates/aither/src/mac.rs
RUST/string-slice:crates/aither/src/wpa.rs
RUST/string-slice:crates/haphe/src/touch.rs
RUST/string-slice:crates/kelyphos/src/stp.rs
RUST/string-slice:crates/kelyphos/src/transport.rs
RUST/string-slice:crates/klesis/src/ccci.rs
RUST/string-slice:crates/klesis/src/pdu.rs
RUST/string-slice:crates/krypta/src/ratchet.rs
RUST/string-slice:crates/krypta/src/x3dh.rs
RUST/string-slice:crates/leipsanon/src/engine.rs
RUST/indexing-slicing:crates/leipsanon/src/engine.rs
RUST/string-slice:crates/pteron/src/transport.rs
RUST/string-slice:crates/stegnos/src/cipher.rs
RUST/string-slice:crates/topos/src/nmea.rs

RUST/as-cast:crates/aither/src/mac.rs
RUST/as-cast:crates/eidolon/src/color.rs
RUST/as-cast:crates/eidolon/src/font.rs
RUST/as-cast:crates/haphe/src/gpio.rs
RUST/as-cast:crates/haphe/src/input.rs
RUST/as-cast:crates/kelyphos/src/stp.rs

# =============================================================================
# Files exceeding 800-line limit — split tracked in #131
# =============================================================================
#
# WHY(#131): 48 files exceed the 800-line ceiling. Each file is a cohesive
# kernel subsystem; splitting 48 at once risks regressions in a bare-metal
# kernel that cannot be tested in CI. Per-file splits tracked in issue #131.
RUST/file-too-long:crates/thumos/**
RUST/file-too-long:crates/aither/src/mac.rs
RUST/file-too-long:crates/kelyphos/src/wmt.rs
RUST/file-too-long:crates/kelyphos/src/transport.rs
RUST/file-too-long:crates/klesis/src/pdu.rs
RUST/file-too-long:crates/pteron/src/hci.rs
RUST/file-too-long:crates/pteron/src/transport.rs
RUST/file-too-long:crates/sema/src/cell.rs

# =============================================================================
# Library crate lib.rs test-presence false positives
# =============================================================================
#
# WHY: Every workspace library crate uses lib.rs solely for module declarations
# and re-exports. Every submodule contains #[cfg(test)] blocks with unit tests
# (verified via `grep -r "#\[test\]" crates/*/src`). The TESTING/no-tests rule
# only scans lib.rs for a test module; the heuristic misses the real tests.
TESTING/no-tests:crates/aither/src/lib.rs
TESTING/no-tests:crates/asphaleia/src/lib.rs
TESTING/no-tests:crates/eidolon/src/lib.rs
TESTING/no-tests:crates/haphe/src/lib.rs
TESTING/no-tests:crates/kelyphos/src/lib.rs
TESTING/no-tests:crates/klesis/src/lib.rs
TESTING/no-tests:crates/krypta/src/lib.rs
TESTING/no-tests:crates/leipsanon/src/lib.rs
TESTING/no-tests:crates/pteron/src/lib.rs
TESTING/no-tests:crates/sema/src/lib.rs
TESTING/no-tests:crates/stegnos/src/lib.rs
TESTING/no-tests:crates/topos/src/lib.rs

# =============================================================================
# fuzz/ — standalone cargo-fuzz crate
# =============================================================================
#
# WHY: cargo-fuzz requires a separate [workspace] declaration; the fuzz crate
# cannot inherit workspace.package, license, or rust-version fields because it
# is outside the main workspace (nightly-only, never published).
MANIFEST/no-workspace-package:fuzz/Cargo.toml
MANIFEST/no-license:fuzz/Cargo.toml
MANIFEST/no-msrv:fuzz/Cargo.toml
MANIFEST/no-deny-toml:fuzz/Cargo.toml
MANIFEST/no-rustfmt:fuzz/Cargo.toml
# WHY: fuzz/corpus/ holds binary fuzzer-input samples (raw protocol bytes),
# which are intentionally not valid UTF-8 and cannot be canonical-clean-linted.
WORKTREE/canonical-clean:fuzz/corpus/**

# =============================================================================
# crates/thumos/ — bare-metal kernel binary, excluded from workspace
# =============================================================================
#
# WHY: thumos is the bare-metal kernel binary (target_os = "none"). It is
# excluded from the workspace so cargo doesn't apply std-target assumptions,
# which means it cannot inherit workspace.package fields. The version is
# maintained independently in this Cargo.toml.
RELEASES/independent-crate-version:crates/thumos/Cargo.toml

# WHY: thumos main.rs contains the ARM boot stub (`core::arch::global_asm!`),
# the panic handler, and 106 kernel module declarations. Moving logic into a
# library crate isn't meaningful here — the modules ARE the kernel, and the
# boot stub + panic handler must live in the bin target with `no_main`.
ARCHITECTURE/thick-binary:crates/thumos/src/main.rs

# WHY: cohesive single-owner kernel structs that exceed the 12-field limit.
# Elf32Ehdr IS the ELF32 header (14 fields, fixed by the ABI — cannot be
# reduced). Process (the PCB), LockScreen, Telephony, and AlarmScreen aggregate
# state whose field-grouping refactor touches every construction site — a
# separate architectural change, not a formatting concern. (Surfaced when the
# kernel was first rustfmt-formatted.)
RUST/struct-too-many-fields:crates/thumos/src/elf.rs
RUST/struct-too-many-fields:crates/thumos/src/lock_screen.rs
RUST/struct-too-many-fields:crates/thumos/src/process.rs
RUST/struct-too-many-fields:crates/thumos/src/screen_alarm.rs
RUST/struct-too-many-fields:crates/thumos/src/telephony.rs

# WHY: kelyphos::wmt defines `RegisterIo` alongside `MmioRegisterIo`, its only
# production impl. The consumer (thumos kernel) is outside the workspace so
# moving the trait into it isn't possible; the other potential consumer is
# the test mock (also in kelyphos). Revisit once thumos gains path deps
# (#126) — until then the colocation is deliberate.
ARCHITECTURE/trait-impl-colocation:crates/kelyphos/src/wmt.rs

# WHY: crates/thumos is excluded from [workspace] (separate license + bare-metal target).
# Excluded crates cannot use version.workspace = true or edition.workspace = true.
# These fields must be explicitly set in the kernel Cargo.toml.
MANIFEST/package-workspace-inheritance:crates/thumos/Cargo.toml

# =============================================================================
# ci.yml — dependency audit lives in the dedicated security.yml workflow
# =============================================================================
#
# WHY: cargo deny + cargo audit run in .github/workflows/security.yml — the
# fleet SUPPLY-CHAIN.md pattern keeps security scanning in a dedicated workflow
# (PRs + a daily schedule so newly-published CVEs against existing deps are
# caught even with no PR open). The CI/no-deny-check rule assumes a single
# consolidated CI file and does not see the sibling security workflow. The
# check genuinely runs; it is just not duplicated in ci.yml.
CI/no-deny-check:.github/workflows/ci.yml

# =============================================================================
# Post-#130 crates — same pattern, relocated code
# =============================================================================
#
# WHY(#719): the #130 block above suppresses RUST/indexing-slicing for the
# parser/encoder files that existed when it was written. The crates below
# postdate it and carry the identical pattern. Two of them carry it because
# convergence work MOVED the code: klesis/src/pdu.rs and klesis/src/gsm7.rs are
# both ignored above, and #662/#700/#710 extracted their BCD framing, GSM-7
# codec, and PDU primitives into klesis-core. The code did not change, only its
# address. An extraction relocates code out from under its suppressions, and
# nothing warns that it has.
#
# Each site was verified in-place before being listed here: every one is either
# an index into a fixed-size array with a literal index (no runtime length
# exists), an index after a length check earlier in the same function that
# returns a typed error first, or a private helper whose every call site is
# guarded. `smp/pdu.rs::decode` is representative -- its `expect(want)` tests
# `body.len() == want`, exact equality, and every decode call sits directly
# behind it.
#
# WHY files, not a `smp/**` glob: a glob would also silence a NEW file in these
# directories, which nobody has read. Listing files means new code gets its own
# review rather than inheriting this one.
#
# NOT a permanent position. kanon#3176 asks for the rule to resolve caller
# guards and fixed-size arrays; if that lands, this whole block and #130's
# should be deleted together rather than left as scar tissue.
RUST/indexing-slicing:crates/klesis-core/src/lib.rs
RUST/indexing-slicing:crates/metaxu-core/src/envelope.rs
RUST/indexing-slicing:crates/metaxu-core/src/protocol.rs
RUST/indexing-slicing:crates/pteron/src/smp/mod.rs
RUST/indexing-slicing:crates/pteron/src/smp/pairing.rs
RUST/indexing-slicing:crates/pteron/src/smp/pdu.rs
RUST/indexing-slicing:crates/pteron/src/smp/toolbox.rs
RUST/indexing-slicing:crates/topos-core/src/lib.rs

# WHY(#719): same lib.rs-only heuristic as the TESTING/no-tests block above.
# metaxu-core's lib.rs is module declarations and re-exports; its submodules
# carry 19 tests (envelope.rs 10, grants.rs 6, protocol.rs 3).
TESTING/no-tests:crates/metaxu-core/src/lib.rs
